# Linimal 残機能実装 作業ログ

## 目的

LINE 26.11.0 向けに、設定画面のカタログ駆動化、残りの UI 抑制機能、外部ブラウザ、手動既読送信を安全な独立パッチとして追加する。

## 進行中

- [x] 設定画面を FeatureCatalog 駆動へ変更
- [x] UI 機能の実 DEX ターゲット解析
- [x] 各 UI 機能パッチと runtime hook の実装
- [x] 外部ブラウザの解析・実装
- [x] outbound read receipt の解析・実装
- [x] Home Performance Adと下部タブと設定階層の調査文書を作成
- [x] Agent i全入口の調査文書を完成
- [x] Config schema v2へ移行
- [x] Shoppingタブ抑制を実装
- [x] Home Performance Adを空白ごと除外
- [x] Agent i入口を場所別に実装
- [x] 設定画面を階層化
- [x] 第1弾バグ修正6件の実装と unit test / buildAndroid / exact APKM / 実機検証
- [x] 第2弾の事前調査と `docs/analysis/home-feed-post-and-premium-row.md` の作成
- [x] 第2弾のドキュメント更新 (README の機能表、docs/analysis、docs/testing/baseline-matrix.md)
- [x] 第2弾 ホーム下部の投稿カード抑制 `linimal.home-feed-post-cards` の実装
- [x] 第2弾 LINE設定のプレミアム行抑制 `linimal.premium-settings-row` の実装
- [x] 第2弾の unit test / buildAndroid / exact APKM / 実機検証

## 制約

- 設定 OFF では LINE の元動作へ戻す。
- 誤一致時は変更せず、PARTIAL / TARGET_NOT_FOUND / ERROR を記録する。
- 難読化クラス名を fingerprint の主要条件にしない。
- 認証・課金・共通通信・TLS・署名検査を変更しない。
- 既読は UI ではなく actual outbound 送信を抑制し、未確認経路があれば完全対応と表示しない。
- APK、DEX 解析出力、patched APK、鍵、資格情報、利用者データをコミットしない。

## 検証結果

- FeatureCatalog 駆動の設定画面: debug Java compile / unit test 成功。
- Premium target 複数一致時の status count 不整合を再現テスト付きで修正。`:patches:test --tests dev.utaa.linimal.patches.features.premium.PremiumUnsendPromotionPatchTest` 成功。
- 通常チャットの outbound 既読 choke point は特定済み。非同期 supplier identity と ThreadLocal を組み合わせ、手動操作の一回だけを worker thread へ伝搬する runtime hook と回帰テストを追加。Square/OpenChat、Service Chat、AI Character は別経路のため全体としては未完了。
- 外部ブラウザの pure policy と fail-open runtime helper を追加。通常リンク専用 caller の fingerprint は追加解析中。policy unit test と debug Java compile は `--no-daemon` で成功。
- 常駐 Gradle daemon が欠損した instrumented plugin JAR を参照していた。キャッシュ削除はせず、単発 `--no-daemon` build で回避できることを確認。
- VOOM / NEWS / Wallet / Chat Calendar・Gift・Pay / LINE AI / Homeおすすめ・話題 / Smart Channel広告を実装。exact APKM で expected=actual、全 status OK。親側レビューで共有predicate未注入時の誤OK、Home metadata複数一致、Smart Channel scratch register検証を追加修正し、`test buildAndroid` 成功。

## 追加要求の調査結果

- Home Performance Ad は Smart Channel ではなく、Home default module catalog の middle / bottom entry を持つ。固定30日resourceはVoIP anti-fraud用でありHome広告とは無関係。
- Home Performance Ad はexpirationやLadDatabaseを変更せず、middle / bottom moduleを表示catalogへ追加しない方式を採用する。
- Shoppingは `COMMERCE` と `COMMERCE_TW` でありWalletとは独立している。
- 設定画面は一Activity内の `General / Agent i / Tabs / Home / Chat / Patch Status` へ階層化する。
- `docs/analysis/home-top-ad.md`、`docs/analysis/main-tabs.md`、`docs/analysis/settings-navigation.md` を作成済み。
- Config schema v2を追加し、旧広告設定をSmart ChannelとHome topへ、旧LINE AI設定を確認済みのAgent iとLINE AI入口7件へ移行する。Shoppingは新規既定値ON、Walletは既定値OFFを維持する。
- Agent iとLINE AIの到達可能な入口はHome header、Chat information、Wallet header、Main Settings、Chat composer、Message context menu、Chat galleryの7種類を確認した。Commerce、Search、Home AI Matomeはnavigationを確認できない候補として実装対象外にした。
- Chat composerはAI Talk visibility observerへgateを追加した。Message context menuはLINE_AI supplier、Galleryはheader binderへ別々のgateを追加した。各targetはexact APKMで一意に確認し、画像編集2入口は局所executeでOK 1/1。
- Configを含むextension unit testは認証環境変数を一時注入した `./gradlew --no-daemon :extensions:linimal:test` で成功。
- 追加レビューでmigration markerが進まないbackendの無限retryと、main tabのList field誤選択余地を確認した。migrationは一段ごとのversion進捗を必須化した。main tabはp0、List parameter、低位localへのcopy provenance、field storeの一意性を検証するよう強化した。exact constructorではp0のv16をv0へ、Listのv22をv1へcopyしてからstoreするため、hookは検証済みv1へ注入する。
- exact LINE 26.11.0 arm64-v8a APKMのpreflightは成功。現在adb接続端末はないため実機再検証は待機中。
- Home GCS Performance Adはview dataのsingleton list作成直後とFlow emit直前の一意targetへgateを追加した。exact applyで `linimal.patch.home-top-ad-module-gate` はOK 1/1。専用attachmentの復元shapeを証明できないためslot collapseは追加せず、上流でrow供給を止める。
- Linimal設定をGeneral、Agent i、Tabs、Home、Chat、Patch Statusへ階層化した。RootにSwitchを置かず、子pageをScrollViewで描画する。header Backとsystem Back、page path保存と不正stateのRoot復元を実装し、extension全テストが成功。
- 統合適用でAgent i Home headerの高位parameter `v26` を非range invokeへ渡す不備を検出した。`invoke-static/range { p1 .. p1 }` へ修正し、patched DEXでもrange callと`move-result v26`を確認した。
- 修正後のexact APKM統合では全33 Patch StatusがOKかつexpectedとactual一致。全14 DEXのdexdumpとd8、extension Kotlin class 0、Settings Activityのexported falseを確認した。
- 最終レビューの必須指摘を反映した。Featureごとの必須PatchId集合をruntimeで完全一致検証し、Agent i Settingsのshape ERRORはraw predicate countを保持する。Context menuとGalleryはregister、branch target、boolean-to-visibility data-flowを追加検証する。
- レビュー修正後もexact APKMで全33 statusがOK。全14 DEXのdexdumpとd8、extension内74 classが全てLinimal配下でKotlin class 0を再確認した。
- Android端末 XIG07、arm64-v8a、SDK 36へ最終APKを上書きinstallできた。
- 初回起動で `rb0.c.invoke()` のDEX verifierが `v0 has type Boolean but expected Reference yj2.c` を報告してcrashした。自動status、dexdump、d8では検出できなかったruntime type-flow不整合である。renderer objectの同型変換へ修正し、cold startとART verifyで解消を確認した。
- Walletタブ初回表示では `WalletV3GrandDesignHeaderView.o()` のoffset `0x1F`で、hook戻り値が`Object`のまま具体型setterへ渡されるVerifyErrorを検出した。`move-result-object`直後に`WalletV3GrandDesignHeaderView$a`への`check-cast`を追加した。
- Wallet修正版はunit testと`buildAndroid`、exact APKM preflight、全33 Patch Status、全14 DEXのdexdumpとd8に成功した。既存と同じ署名鍵でXIG07へ上書きinstallし、ミニアプリタブでWallet headerをinflateしてもprocessを維持した。Wallet VerifyErrorとFATAL EXCEPTIONは0件で、ART全DEX verifyも成功した。

## 2026-09-02 実機レビューでの指摘 8件

利用者が patched LINE 26.11.0 を XIG07 で確認し、次の指摘を受けた。1〜4、7、8をバグ修正の第1弾、5と6を新機能の第2弾とする。

### 第1弾 バグ修正

1. Smart Channel の枠が残る。トグルONでも `smart_ch_content_view` が表示され、`banner_container_root` が365px占有して下が詰まらない。方針は枠ごと非表示。
2. Linimal設定画面でシステムActionBarの `Linimal` ラベルが自作ヘッダーへ重なる。manifestの `Theme.DeviceDefault.Settings` と `android:label` が原因。ヘッダーの戻るボタンがタップできない実害もある。
3. トーク下部の `chat_ui_ai_talk_suggestion_chip_bar` が残る。既存パッチは入力欄内のImageViewだけをgateしており、チップバーは対象外だった。
4. トーク一覧の検索欄右のAgent iボタンが未実装。既存7入口に含まれていない。
7. LINE設定の `Agent i` 行が消えない。`Agent I Settings` は適用済み(2/2)だが実行時に抑制されていない。
8. 2階層目でシステムバックするとRootを飛ばしてLINE設定へ戻る。predictive back環境で `onBackPressed()` が呼ばれないため。

### 第2弾 新機能

5. ホーム下部のVOOMおすすめ投稿カードを、見出し行ごと消せるようにする。
6. LINE設定の `プレミアム 未登録` 行を消せるようにする。

### 決定事項

- Smart Channelは広告判定に頼らず枠ごと非表示にする。LINE自社の告知も消える。
- ホーム下部の動画は投稿カードごと消す。
- 第1弾を先に実機へ出し、確認後に第2弾へ進む。

### 検証環境

- 実機 XIG07、arm64-v8a、SDK 36、LINE 26.11.0 patched が install 済み。
- 逆アセンブルは `dexdump -d` を全13 DEXへ実行して grep する方式。
- ビルドは `GITHUB_ACTOR=uta-a GITHUB_TOKEN=$(gh auth token) ./gradlew --no-daemon test buildAndroid`。

### 第2弾の事前調査

- LINE設定のプレミアム行は `layout/line_user_settings_premium_item` (0x7f0e0570)。参照は `Lay4/e;.<clinit>` と `Lpx4/m0;.<init>` の2箇所のみ。
- ホーム下部の動画カードは resource-id を持たず、content-desc が `...の動画. ... ダブルタップして詳細ページに移動` で、`友だち追加` と `メニューを開く` を伴う。Compose 製の可能性が高い。
- Homeページのトグルは 上部広告 / おすすめ / 話題 の3件がすべてONだが動画カードは残るため、既存の HOME_RECOMMENDATIONS と HOME_TRENDING とは別枠である。
- 事前調査の結論は `docs/analysis/home-feed-post-and-premium-row.md` へまとめた。ホーム投稿カードは `GcsHomeFeedPostModuleController` の source metadata で、プレミアム行は layout `line_user_settings_premium_item` と2件の title resource で特定する。

### 第1弾の実装と検証結果

- 5件を並行して実装した。設定画面、Smart Channel、chip bar、設定Agent i行、トーク一覧Agent iボタン。
- `AgentISettingsPatch` が実機で効かなかった真因は注入位置だった。dexlib2 の `addInstruction(index, insn)` は新しい `MethodLocation` を挿入し、既存 location が `Label` を保持したまま後ろへずれる。boxing の合流点へ注入したため `goto` が hook を飛び越え、true 経路がそのまま `Boolean.TRUE` を返していた。注入位置を唯一の `return-object` 直前へ移し、unbox と再 box を伴う命令列へ変更した。分岐先アドレスと一致しないことを shape 検証へ追加した。
- Smart Channel は枠 `SmartChannelViewLayout` を GONE にする UI state gate を追加した。`banner_container_root` は instant news、beacon、LAN banner と共有のため消さない。残るのは root の paddingBottom 6dp のみで、これは Smart Channel 未配信時の素の LINE と同じ状態である。
- chip bar は `Lgs1/i0;` binding の field `b` へ入る ComposeView で、presenter 生成の直前で null へ差し替える。LINE 自身が chip bar 無効構成で通る `if-eqz` 分岐へ入るため、setContent も setVisibility も実行されない。
- トーク一覧の Agent i は Compose 側の検索行にあり、`onClickSearchBarAiButton` の debug metadata で一意に特定した。旧 View 実装の `ChatListFragment` 経路は Home や NEWS と共有のため対象外にした。
- `LineAiGalleryViewerPatch` と `LineAiMessageContextMenuPatch` は既に分岐先チェックを持つ。他パッチへの横展開は実機確認で効いていない機能を洗い出してから判断する。
- `./gradlew --no-daemon test buildAndroid` 成功。
- preflight は build-tools 37.0.0 の apksigner でのみ成功する。36.1.0 と 36.0.0 は base.apk の v3.2 を報告せず失敗する。
- `--verify-with-sdk` を付けると Morphe の cross-DEX 検証が19件を報告して停止する。すべて LINE 本体に元からある Wear SDK と Sentry の optional 依存で、元 APK の全 DEX にも定義がない。Linimal の変更とは無関係のため、当該オプションを外して適用し、dexdump と d8 を手動で実行する。
- 適用後の検証は次のとおり。埋め込み Patch Status 34件すべて OK で expected と actual が一致。全14 DEX の `dexdump -f` 成功。`d8 --min-api 32` 成功。新規 DEX は 114 class で Linimal 82 と strip された LINE 本体 32、Kotlin class は 0。

### 第1弾の実機検証結果 2026-09-02

XIG07 へ上書き install した。install、ART 全DEX verify、cold start はすべて成功し、FATAL EXCEPTION と VerifyError は 0 件だった。

| 指摘 | 結果 | 実測 |
| --- | --- | --- |
| 1 Smart Channel の空白 | 解消 | `smart_ch_content_view` が消え、`banner_container` は `[0,438][1220,458]` の20pxのみ。トーク一覧の先頭が y=803 から y=458 へ 345px 上へ詰まった |
| 3 chip bar | 解消 | `chat_ui_ai_talk_suggestion_chip_bar` が消え、入力欄が `[404,2370]` から `[404,2514]` へ 144px 下がった |
| 4 検索欄右の Agent i | 解消 | ボタンのノードが消え、検索ボックスの右端が 1036 から 1167 へ広がった |
| 7 設定の Agent i 行 | 解消 | 個人情報が 年齢確認 で終わり、バックアップ・引き継ぎ が上に詰まった |
| 8 システムバック | 解消 | Chat ページから Root ページへ戻る |
| 2 ラベルの重なり | 解消 | inset 修正後にヘッダーが `[0,138][1220,320]` へ収まり、戻るボタン `[0,151][156,307]` のタップで Chat から Root へ戻る |

指摘2は最初の適用では部分解消だった。ActionBar なしテーマにしたことで edge-to-edge になり window inset が content へ適用されず、ヘッダーが status bar と重なって戻るボタンが押せなかった。root へ `WindowInsets.Type.systemBars()` と `displayCutout()` の inset を padding として入れて解消した。

再適用後の確認では、Patch Status 34件すべて OK、install と cold start 成功、Chat から system back で Root、Root から system back で LINE 設定への遷移、ヘッダー戻るボタンでの Chat から Root への遷移をいずれも確認した。第1弾の6件はすべて解消した。

### 第2弾の実装と検証結果 2026-09-02

- 2機能を並行実装し、共有設定、Patch Status、依存チェーン、ドキュメントを別々に統合した。依存チェーンは `agentIChatListSearch → homeFeedPostCards → premiumSettingsRow → noOpProbe` の直列にした。
- 前段の実装は2機能とも既存設定を流用しており、独立して切り替えられなかった。投稿カードは Home おすすめ、プレミアム行は Premium 誘導の設定を読んでいた。どちらも専用キーへ差し替えた。`linimal.ui.home.feed_post_cards` と `linimal.settings.premium_row` を追加し、schema version は 2 のまま既定値へ落ちる経路を使う。
- 投稿カードの初回適用は Patch Status が ERROR になった。真因は注入前の安全検査である。dexlib2 の 35c は未使用 slot の getter が 0 を返すため、5 slot すべてを見ると引数の少ない invoke がすべて scratch register `v0` の read と誤判定される。実引数の個数までに限定して判定するよう修正した。
- 修正後は OK になったが、実機ではカードが消えなかった。設定を ON と OFF で切り替えてフィードを比較しても内容が変わらず、対象が誤りだと確定した。Home 下部の動画カードを描くのは `GcsHomeFeedUnitSingleModuleController` と `GcsHomeFeedUnitBigVisualModuleController` である。
- card renderer の呼出しを飛び越す方式をやめ、`Composer.shouldExecute` の結果だけを書き換えて LINE 自身の `skipToGroupEnd` 経路へ倒す方式に作り直した。新しい control flow を作らないため composer group の整合が変わらず、戻り値が `Z` なので非抑制時に 1 へ戻しても情報が失われない。3件の module controller すべてに同じ形で適用し、1件でも解決できなければ何も変更しない。
- Morphe 適用は既定の鍵ストアを使うと端末の署名と一致しない。`--keystore /Users/uta_a/morphe/linimal.keystore` を明示する必要がある。
- 最終適用の検証は次のとおり。Patch Status 36件すべて OK で expected と actual が一致。投稿カードは OK 3/3、プレミアム設定行は OK 2/2。全14 DEX の `dexdump -f` と `d8 --min-api 32` 成功。install、ART 全DEX verify、cold start 成功、FATAL EXCEPTION と VerifyError は 0件。
- 実機では Home 下部の動画カードと友だち追加の見出し行が消え、LINE 設定から LYPプレミアム行が消えて下が詰まった。両機能とも OFF で元表示へ復元することを確認し、設定を ON へ戻した。
- Home下部に残った広告は Performance Ad ではなく `GcsAdModuleController` が `gcs_ad_section` へ描く汎用GCS Adだった。同じHome広告設定で専用singleton item listを空にするgateを追加し、catalog、Performance Ad Flow、汎用GCS Adの全targetを注入前に検証するall-or-none構成へ拡張した。Patch Status 38件、全14 DEX、install、ART verify、cold startが成功し、Home初回、タブ往復、refresh後の広告ノードはすべて0件だった。

### 第3弾 ホーム特集枠と注入位置の是正 2026-09-02

- ホームに残っていた「i-dle特集」の 2列グリッドは Home26 Feed の ShortFormGrid module だった。カードの読み上げ文字列 `%sの動画` と各カードの「メニューを開く」を出力する Compose ファイルが一致し、見出しはサーバー供給の文字列 field である。matome とも投稿カード3件とも別 module のため、話題を OFF にしても変化しなかったのは整合的である。
- 新設定 `linimal.home-featured-collections`「特集枠を表示しない」を Home ページへ追加した。既定値 ON。抑制方式は投稿カードと同じ `Composer.shouldExecute` の書き換えである。
- 既存パッチの実効性を全件監査した。注入位置が既存の分岐先と一致するのは既読抑制の2箇所だけだった。
- 既読抑制の outbound gate は分岐の合流点そのものへ注入していた。`Z` が false の経路では gate 全体を飛び越すため、将来 LINE 側が false 呼び出しを追加した時点で静かに無効化される潜在バグだった。合流点の直後へ移し、分岐先および例外 handler 先頭との一致検証を追加した。
- 既読抑制の manual caller は `<any>` handler の先頭へ cleanup を注入していた。handler ラベルは既存 location に残るうえ直前が `return-object` でフォールスルーが無く、`clearManualInvocation()` が一度も実行されていなかった。非 Cancellation 例外で手動フラグが残留し、以後の自動既読が手動と誤認されて送信されうる fail-open だった。handler 先頭の直後へ移した。どちらも再現テストを先に追加し、修正前の値へ戻すと失敗することを確認した。
- `HomeTrendingPatch` を子描画の飛び越しから `shouldExecute` 方式へ移行した。旧方式は hook の戻り値で制御フローが変わるため、Home を composed のまま設定を切り替えると slot table 不整合を起こしうる構造だった。label resource の並び検証と compose group key による一意化は失われるが、どちらもバージョン追従性の低い anchor であり、同定は module controller の source metadata が担う。
- 検証結果。Patch Status 39件すべて OK で expected と actual が一致。全14 DEX の `dexdump -f` と `d8 --min-api 32` 成功。install、ART 全DEX verify、cold start 成功、FATAL EXCEPTION と VerifyError は 0件。
- 実機のホームは プロフィール、サービス、友だちリスト だけになり、4画面ぶんスクロールしても特集、動画カード、メニューを開く、広告、友だち追加のノードはすべて 0件だった。特集枠は OFF で見出し1件と動画4件が復元することを確認し、設定を ON へ戻した。トーク一覧の回帰も確認した。
- リポジトリ直下に JADX 出力 `base/sources/tf2/j.java` が残っていたため削除し、`.gitignore` へ解析出力先を追加した。

## 次アクション

### 完了済み

- 第1弾のバグ修正6件は実装、検証、利用者の実使用確認まで完了した。
- 第2弾の2機能とHome広告の拡張は実装、unit test、`buildAndroid`、exact APKM 適用、Patch Status 38件 OK、全14 DEX 検証、XIG07 での ON / OFF 実機確認まで完了した。
- 実機の APK から直接読んだ Patch Status と第2弾およびHome広告拡張の実機検証結果は `docs/testing/baseline-matrix.md` へ反映済み。
- JADX 出力の `base/` を削除し、同種の解析出力を `.gitignore` で除外した。逆コンパイル結果はリポジトリへ含めない。

### 残作業

- コミットが未実施である。第2弾の実装、設定、Patch Status、ドキュメント更新をまとめてコミットする。
- プレミアム設定行は実機で LYP variant のみ表示されるため、LINE variant が未確認である。
- login / session restore の検証が未実施である。端末が未登録状態のため、隔離テストアカウントが必要になる。
- Home 広告は初回、refresh、タブ往復、4画面ぶんのスクロールまで確認済み。background 復帰と process recreation 後の確認が残る。
- ホームに残る「i-dle特集」系モジュールの抑制が未着手である。対象の特定は親側で別途調査中。

### 補足

- 効いていない抑制が見つかった場合は注入位置の分岐先問題を疑う。
- 全実機確認とコミットの完了後に、一時生成物とこの作業ログを片付ける。
